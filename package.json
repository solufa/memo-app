{
  "name": "memo-app",
  "version": "1.0.0",
  "description": "My astonishing Nuxt.js project",
  "author": "m_mitsuhide",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "deploy": "nuxt generate && firebase deploy --project memo-db26a",
    "login": "firebase login",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.0.0",
    "bootstrap-vue": "^2.0.0-rc.11",
    "express": "^4.15.3",
    "firebase": "^5.3.1",
    "nuxt": "^1.0.0"
  },
  "devDependencies": {
    "cross-env": "^5.0.1",
    "firebase-tools": "^4.0.3",
    "nodemon": "^1.11.0"
  }
}
